{
  "name": "@google/gemini-cli-swe-agent-tests",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "vitest run --reporter=dot --test-timeout=120000",
    "preflight": "npm test"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
